@model CreateOrUpdateNewsViewModel
@{
    ViewData["Title"] = "Create News";
    Layout = "_Layout";
}

<div class="card card-primary">
    <div class="card-header">
        <h3 class="card-title">Create News</h3>
    </div>
    <form asp-controller="News" asp-action="Create" asp-route-returnurl="@ViewData["ReturnUrl"]">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="card-body">
            <span asp-validation-for="Title" class="text-danger"></span>
            <div class="form-group">
                <label for="inputCreateNewsTitle">
                    @Html.DisplayNameFor(vm => vm.Title)
                </label>
                <input asp-for="Title" type="text" class="form-control" id="inputCreateNewsTitle" placeholder="Title">
            </div>
            <span asp-validation-for="Content" class="text-danger"></span>
            <div class="form-group">
                <label for="inputCreateNewsContent">
                    @Html.DisplayNameFor(vm => vm.Content)
                </label>
                <input asp-for="Content" type="text" class="form-control" id="inputCreateNewsContent" placeholder="Content">
            </div>
        </div>
        <div class="card-footer">
            <button type="submit" class="btn btn-primary">Create</button>
        </div>
    </form>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
